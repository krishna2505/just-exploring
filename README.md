# my-first

A python utility that ingests CSV  or JSON, validates record, logs errors, and writes cleaned outputs.

## Setup
'''bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
'''
## Usage
python3 src/myproject/main.py
